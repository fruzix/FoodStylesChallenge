import {Theme} from '@Ui/Theme/theme';

const useAppTheme = () => {
  // const scheme = useColorScheme();
  //
  // return scheme === 'dark' ? LightTheme : DarkTheme;

  return Theme;
};

export default useAppTheme;
